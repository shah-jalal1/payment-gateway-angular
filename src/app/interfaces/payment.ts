export interface Payment {
  _id?: string;
  id: string;
  amount: number;
  // slug: string;
  // shortDesc: string;
  // longDesc: string;
  // date?: any;
  // dateString?: any;
  createdAt?: string;
  updatedAt?: string;
  select?: boolean;
}
